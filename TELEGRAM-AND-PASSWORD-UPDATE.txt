WAYNE LOGS - TELEGRAM PASSWORD HELP UPDATE

- The login page tells customers to contact the administrator on Telegram if they forget their password.
- It uses the Telegram URL already saved under Admin > Settings.
- The blue Telegram paper-plane icon is animated and adapts to phones.
- The old floating Telegram button is hidden only on Login to prevent duplication.
- Temporary passwords are generated securely at 16 characters.
- Every temporary password contains uppercase letters, lowercase letters, numbers and symbols.
- The one-use login and mandatory private-password change remain enforced.

DEPLOY IN 3 STEPS
1. Replace all project files with this update. Keep your private server .env.
2. Run git add -A, git commit -m "Add animated Telegram password help", then git push.
3. Wait for Render and Vercel, refresh Login, and confirm your Telegram link is saved in Admin > Settings.
