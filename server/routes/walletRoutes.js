const r=require("express").Router(),c=require("../controllers/walletController"),{protect,adminOnly}=require("../middleware/authMiddleware");
r.get("/",protect,c.summary);r.get("/transactions",protect,c.history);r.get("/transfers/recipient/:wayneId",protect,c.lookupTransferRecipient);r.post("/transfers",protect,c.transfer);r.post("/topups/initialize",protect,c.initialize);r.post("/topups/verify",protect,c.verify);r.post("/topups/:reference/submit",protect,c.submitManual);r.get("/admin/transactions",protect,adminOnly,c.adminAll);r.get("/admin/transactions/:id/receipt",protect,adminOnly,c.adminReceipt);r.patch("/admin/transactions/:id/review",protect,adminOnly,c.adminReview);r.get("/admin/transfer-pin",protect,adminOnly,c.adminTransferPinStatus);r.post("/admin/transfer-pin",protect,adminOnly,c.setAdminTransferPin);r.post("/admin/credit",protect,adminOnly,c.adminCredit);r.post("/admin/clear",protect,adminOnly,c.adminClear);module.exports=r;
