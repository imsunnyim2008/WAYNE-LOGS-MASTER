const r=require("express").Router(),c=require("../controllers/walletController"),{protect,adminOnly}=require("../middleware/authMiddleware");
r.get("/",protect,c.summary);r.get("/transactions",protect,c.history);r.post("/topups/initialize",protect,c.initialize);r.post("/topups/verify",protect,c.verify);r.post("/topups/:reference/submit",protect,c.submitManual);r.get("/admin/transactions",protect,adminOnly,c.adminAll);r.patch("/admin/transactions/:id/review",protect,adminOnly,c.adminReview);r.post("/admin/credit",protect,adminOnly,c.adminCredit);module.exports=r;
